# udev rules for Focusrite USB audio interfaces
# Grants access to all Focusrite devices (VID 1235) for logged-in users.
#
# Install to: /etc/udev/rules.d/99-focusrite.rules
# Then reload: sudo udevadm control --reload-rules && sudo udevadm trigger

SUBSYSTEM=="usb", ATTR{idVendor}=="1235", MODE="0666", TAG+="uaccess"
